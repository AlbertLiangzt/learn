多分类
逻辑回归是多分类的一种特殊形式
公式见formula.png

数据准备train.csv——数字识别
第一列：标签，0-9的数字
其他列：对应的特征值（图片）

机器学习中会看到损失函数会添加一个额外项，避免出现强特征覆盖弱特征
L1正则-L1范数
L2正则-L2范数
使用L1正则化的模型——Lasso回归
	产生一个稀疏矩阵，可用于特征选择
	1.系统实现：大大减少参数存储空间
	2.针对性强：泛化能力好
	（3.一定程度上也可以防止过拟合）
使用L2正则化的模型——Ridge回归（岭回归）
	可以防止模型过拟合（overfitting）

稀疏矩阵

 